import { AppMatchPasswordsDirective } from './app-match-passwords.directive';

describe('AppMatchPasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMatchPasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
